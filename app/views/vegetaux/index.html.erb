<h1>Listing vegetaux</h1>

<table>
  <thead>
    <tr>
      <th>Image</th>
      <th>Nom</th>
      <th>Nom common</th>
      <th>Famille</th>
      <th>Classe</th>
      <th>Genre</th>
      <th>Espece</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @vegetaux.each do |vegetable| %>
      <tr>
        <td><%= image_tag vegetable.photo.url(:thumb) %></td>
        <td><%= vegetable.nom %></td>
        <td><%= vegetable.nom_common %></td>
        <td><%= vegetable.famille.nom %></td>
        <td><%= vegetable.classe %></td>
        <td><%= vegetable.genre %></td>
        <td><%= vegetable.espece %></td>
        <td><%= link_to 'Show', vegetable %></td>
        <td><%= link_to 'Edit', edit_vegetable_path(vegetable) %></td>
        <td><%= link_to 'Destroy', vegetable, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Vegetable', new_vegetable_path %>
